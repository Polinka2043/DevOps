events {
    worker_connections 1024;
}
services:
  nginx:
    image: nginx:latest
    ports:
      - "8080:80"
    restart: always
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - my-network

networks:
  my-network:
    external: true
